services:
  - type: web
    name: clinica
    runtime: node
    buildCommand: npm install && npm install --prefix frontend && npm run build
    startCommand: npm start
    envVars:
      - key: APP_PASSWORD
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: DATA_DIR
        sync: false
