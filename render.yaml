services:
  - type: web
    name: clinica
    runtime: node
    buildCommand: npm install && npm install --prefix frontend && npm run build
    startCommand: npm start
    envVars:
      - key: DEMO_MODE
        value: "true"
      - key: APP_PASSWORD
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: DATA_DIR
        sync: false
